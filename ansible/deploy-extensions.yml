---

# To run: ansible-playbook -v -i inventories/vagrant deploy-extensions.yml

- hosts: mainserver
  sudo: true
  vars_files:
    - "vars/common.yml"
    - "vars/environment-specific/{{ deployment_environment_id }}.yml"
    - "{{ secrets_file_path }}"
  roles:
    - { role: ckan-extension, ckanext: ckanext-apicatalog_scheming }
  handlers:
    - include: handlers/handlers.yml
